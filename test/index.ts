import { expect } from 'chai';
import 'mocha';

describe('Routing Tests', function () {
    describe('Expected route returns 200', function () {

        it('GET /{accountId}', async () => {
            expect(200).to.equal(401);
        });
    });
});
